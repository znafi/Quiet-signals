rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Prototype rules: open reads/writes so test submissions work from any device.
    // Tighten this before collecting real user data outside the prototype group.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
